# 08 — Decisões e Pendências

> **Para quem é:** quem retoma o projeto ou precisa decidir algo, e quer saber o que já foi decidido (e por quê) e o que ainda está em aberto.
> **Quando consultar:** antes de propor uma mudança de rumo ou reabrir uma discussão.

---

## Parte 1 — Decisões já tomadas (e o porquê)

| # | Decisão | Por quê |
|---|---|---|
| D1 | **Estender o fork do Open WebUI** (não criar do zero) | Reaproveita login, usuários, Storage, RAG, UI. Custo aceito: fork mais pesado de manter. |
| D2 | **Roteamento por um "motor invisível" (ChatND)** em vez de o usuário escolher o modelo | Simplicidade para o usuário; ele só vê uma IA, a da Nidum. |
| D3 | ~~**RAG modo documento-inteiro** (não só trechos)~~ — **SUPERADA em 16/07/2026** | *Registro original:* trechos davam respostas fragmentadas/incompletas; doc inteiro garante completude; custo (~US$0,12/pergunta) aceito. **O que mudou:** o documento inteiro **competia com a busca afinada e estourava o orçamento** — medido, **159.849 de 200.000** caracteres consumidos por três documentos inteiros, sobrando quase nada para os trechos. `MAX_DOCS_INTEIROS` passou a `0` no `chatnd 1.23.0` (commit `e7232ec2`, 16/07/2026) e a máquina segue no código, desligada. Descrição corrente em `03_Arquitetura_e_Motor.md`. **Em aberto, e não decidido aqui:** se a completude que o D3 buscava deve voltar por outro mecanismo — a Fase D (enumeração) ataca o mesmo problema por um caminho diferente. |
| D4 | **Layout dos arquivos no código da ferramenta**, não em prompt | Garante identidade da marca consistente; prompt/RAG não estilizam arquivo. |
| D5 | **Wrappers privados + só a ChatND pública** | Usuário comum vê só a ChatND; motores ficam ocultos. |
| D6 | **Controle de acesso por aprovação manual** (não OAuth) | `nidumbrasil.com.br` não é Google Workspace; e-mails não são contas Google. Admin aprova só `@nidumbrasil.com.br`. |
| D7 | **Nunca revelar o LLM** ao usuário | Identidade: é "a inteligência da Nidum", não "OpenAI/Anthropic". |
| D8 | **Geração de imagem via Gemini** (`imagen-4.0-generate-001`, predict) | O modelo padrão do Open WebUI dava 404; este funciona com billing pago. |
| D9 | **Tríade fonte/forma/fluxo só quando aplicável** (gate no classificador) | Respostas pareciam "treinamento corporativo". A tríade é da própria Fonte (*Silêncio, Vida e Liberdade*), mas só cabe em pedidos gerativos. |
| D10 | **Editorial: estender o fork, headless (API+chat), Storage S3 ao escalar** | Precisa de endpoints/DB/jobs; livros estouram o volume de 500 MB. |
| D11 | **Editorial: evitar libs AGPL** (PyMuPDF, ebooklib) | Risco para uso comercial; EPUB é montado/lido com zipfile+XML. |
| D12 | **Continuidade: tudo no nome da empresa, chaves no Bitwarden** | A operação precisa sobreviver à saída de qualquer pessoa. |
| D13 | **Migração de e-mail adiada** | Tudo está no `nidum.tec26@gmail.com` (provisório) até haver e-mail institucional. Recomendação aceita: usar e-mail genérico de função, não pessoal. |
| D14 | **Ibrand para títulos** (além do logotipo) | Decisão do usuário (o brandbook reserva Ibrand ao logotipo, mas foi sobreposto); Ibrand tem cobertura PT completa. |
| D15 | **Base em 7 coleções por tipo** (`nd-atas`, `nd-projetos`, `nd-fonte`, `nd-normas`, `nd-marca`, `nd-contratos`, `nd-externo`), saindo de 2 (FONTE/ACERVOS) | Permite roteamento de busca por tipo (busca só nas coleções relevantes) e etiquetas de procedência. `nd-externo` isola docs públicos de terceiros (IPPUL: leis municipais). |
| D16 | **Regra de destino: CONTEÚDO decide, NOME confirma, PASTA desempata**; soberania de pasta `FONTE/`→`nd-fonte` e `IPPUL/`→`nd-externo` **acima** do conteúdo | Procedência inviolável (uma ata citada dentro da doutrina não vira `nd-atas`; lei de Londrina não entra em coleção interna). Fonte única: `esteira/_scripts/colecao_destino.py`. |
| D17 | **`cte_/ct_/cc_/ce_/ger_/ata_` → `nd-atas`** (ata de comitê); `cte_` **nunca** `nd-contratos` | `cte_` é Comitê Técnico (ata), não contrato. `nd-contratos` só por nome (contrato/acordo/estatuto/cnpj) ou pasta jurídica. |
| D18 | **Exclusão de indexação** (`testes/`, `Chico/1 - Cadastros`, backups `antes-da-correcao`, stubs <80 chars, OCR corrompido) — nada sai do repo | Gabaritos, dado pessoal e lixo não devem poluir a base; a exclusão é só de indexação, reversível. Sem-regra vai para `nd-normas` com warning visível. |
| D19 | **Espelho nas 2 coleções antigas tem PRAZO** | Durante a transição, arquivo novo é indexado na coleção nova **e** nas 2 antigas (rede de rollback). Após o aceite da fase, as antigas **param de RECEBER** novos; **nunca são apagadas**. |
| D20 | **Pasta = "projeto"** (instruções + coleções por pasta), com o seletor de Conhecimento do modal de Pasta restrito a **coleções** (prop `collectionsOnly`, PR #36, 2026-08-23) | O recurso já existia no fork (OWUI 0.9.6: `folder.data.system_prompt` + `files`, que o upstream chama de *Folder "Project" handling*) — não foi construído, foi habilitado. A restrição existe porque a pasta aponta para **coleção inteira, nunca para arquivo solto**: com a leitura liberada ao grupo, o `GET /knowledge/search/files` passou a listar um a um os arquivos das coleções no seletor. **CUSTOMIZAÇÃO DE CORE** (`Knowledge/KnowledgeSelector.svelte`, `Models/Knowledge.svelte`, `Folders/FolderModal.svelte`): vai conflitar em **todo bump de versão do fork** — ver a REGRA DO REBASE no `CLAUDE.md`. Pastas são **individuais** (tabela `folder` tem `user_id` e nenhum `access_control`); projeto compartilhado por equipe seria desenvolvimento novo. |
| D21 | **Coleções institucionais em Privado com grant de LEITURA ao grupo Coautores** (não públicas, não escrita) | O usuário seleciona e usa, mas não adiciona nem remove arquivo: todas as rotas de mutação (`/file/add`, `/file/remove`, `/{id}/update`, `/{id}/delete`, `/{id}/reset`) exigem `permission='write'`; o export é admin-only. **EFEITO ALÉM DO SELETOR:** o pipe chama `filter_accessible_collections` antes de buscar — quem ficar fora do grupo perde o RAG da rota `documentos` **em silêncio** (log `nenhuma colecao de conhecimento acessivel`, resposta genérica em vez de erro). Ao mexer nos grants, conferir a cobertura do grupo. |
| D22 | **Consumidor generativo nas duas pontas: o que precisa ser exato é imposto pelo código, não pedido no texto** (03/09/2026) — vale para toda ferramenta nova daqui em diante | As ferramentas do ChatND foram desenhadas para um consumidor **determinístico**: o pipe montava a entrada por schema e repassava a saída *verbatim*. No laço agêntico o **modelo** escreve a entrada e transcreve a saída, e os três defeitos medidos na Fase A são o mesmo deslocamento, não três bugs: (1) o modelo reescreveu o link de download sem a barra inicial → 404; (2) mandou slides sem campo de corpo → PPTX mudo; (3) escreveu CSS próprio que venceu o da marca por cascata. **Evidência direta:** mesmo pedido, mesmo modelo, mesma base — o pipe gerou 18 slides com corpo cheio, o agente 10 slides mudos. A única diferença era **quem descreve a interface** (schema por extenso no prompt × docstring com 4 dos 8 tipos e corpo "opcional"). **Consequência de projeto:** link absoluto em vez de caminho relativo; guarda de cascata em vez de convenção; validação que recusa alto em vez de campo opcional; alias com telemetria e prazo em vez de tolerância permanente. É a mesma regra que pôs **as cores no código e o julgamento na skill** (D4). Corolário: **descrição de ferramenta é código** — muda com PR, entra na doc viva e tem caso de teste. |
| D23 | **A parede `geral` × `documentos` vale também para o agente — e o mecanismo é procedência declarada por afirmação, não separação de ferramentas** (03/09/2026, verificada no A.5-bis) | O FIRMADO do `CLAUDE.md` prevalece sobre a §3.2 do prompt-mestre. Mas numa arquitetura **sem rotas** a parede muda de forma: não é "só uma fonte por resposta", é **"não confunda a origem"**. O agente pode usar base e web na mesma resposta, em blocos separados e rotulados; o que é proibido é misturar as duas dentro da mesma afirmação ou parágrafo. **Verificado com a ferramenta na mão** (não por ausência dela): pedido que forçava a mistura — "compare o modelo de cooperativa que já definimos com o que a legislação brasileira mudou" — produziu seção interna, seção externa, e tabela com as colunas rotuladas; nenhuma afirmação misturada. **Ganho não previsto:** identificou uma contradição jurídica que nenhum dos lados continha sozinho (o modelo interno fala em folha CLT com encargos; a Lei 12.690/2012 diz que cooperado não tem vínculo), ligou a uma pendência já aberta nas atas e propôs três estruturas. A parede **não** custou capacidade analítica. **Regra da query (a mais importante):** conteúdo interno NUNCA entra no texto da busca externa — a query sai da casa. Medido: três buscas externas, zero menção à Nidum; a busca interna usou "modelo de cooperativa Nidum", as externas só termos públicos. **Destino do `ENABLE_WEB_SEARCH=False`:** era contorno técnico para contaminação pré-pipe (o caminho é pulado em Native, `middleware.py:2578`), não política — a decisão de 03/09 é ligar. E o valor efetivo mora no **banco**, não na env var: a variável só semeia a primeira carga (3ª vez que esse padrão aparece; ver D24). |
| D24 | **O valor efetivo de configuração mora no BANCO; env var e default de código só semeiam a primeira carga** (03/09/2026) | Apareceu **três vezes** no mesmo dia, sempre custando investigação: (1) `DIAL_FASE3` tem `default=False` no código e estava **ligado** em produção; (2) `BASE_CONHECIMENTO_ID` apontava para duas coleções apagadas enquanto o default do código era `""`; (3) `ENABLE_WEB_SEARCH=True` no Railway **não** ligou a busca — o interruptor "Pesquisa na Web" em Admin → Configurações estava desligado no banco, e o banco tem precedência. **Consequência:** ler o código NÃO diz o que está no ar, e nenhuma das três divergências deixa rastro em commit. Toda pergunta sobre estado corrente se responde no **painel**, não no repositório. É a origem comum dos treze registros vencidos (ver a lista no plano da Fase A) e a justificativa do `conferir_registros.py` — que precisa conferir também **o que a esteira carimba nos dados**, não só o que a doc afirma. |
| D25 | **A esteira protege só a SAÍDA. A premissa mudou: ingestão indevida também é irreversível — e passa a exigir freio de ENTRADA** (03/09/2026) | Os três freios da esteira calculam **sobre remoção**, sem exceção: `puxar_sharepoint.freio_disparou(**remover**, …)` (nº1), `sincronizar.freio_unificado(**removidos**, …)` — todos os ramos, `ESVAZIAR`/`CATASTROFE`/`EVENTOS` (nº2) — e `higiene_owui.freio_higiene(**n_exclusoes**, …)` (nº3). A `guarda_entrada`, apesar do nome, também é saída: pega "repo vazio + base cheia", que é remoção em massa disfarçada. **O único controle que olha a entrada é `relato_topo_ignorado`, e o próprio código diz: _"Nao bloqueia: e um aviso de escopo, nao um freio"_.** A assimetria não é esquecimento — é desenho replicado três vezes de propósito (`higiene_owui:60` diz "estilo dos freios nº1 e nº2"), e era **certa** quando a esteira nasceu do incidente da base zerada: o risco era **perder** dado. **O que mudou:** o risco virou **ingerir** o que não devia. E ingestão **também é irreversível** — o arquivo sai do índice, mas a leitura não se desvê: quem viu a carta ao presidente viu, e a resposta já dada não se recolhe. Os dois casos de 2026 são de entrada, não de saída: os 7 documentos "NÃO CIRCULAM" (17/08) [**CORRIGIDO em 04/09 — ver D32: este caso era falso positivo; o material e interno legitimo e os sete ficam**] e o risco vivo de `testes/` sair da exclusão na reformulação das pastas, que faria o ChatND responder com o gabarito do próprio banco de provas. **Feito:** `guarda_pastas_soberanas` no portão de abertura do `sincronizar` — trava a **indexação**, nunca o espelho do repo (a regra 9 se mantém: o SharePoint continua sendo a fonte da verdade). **Escopo da assimetria:** é da **esteira**, não do projeto — o pipe tem orçamento de entrada (`MAX_CHARS_TOTAL`, `MAX_CHARS_ANEXO`), a tool valida entrada (`_diag_entrada_vazia`, slide sem corpo) e o laço tem teto de iterações. **Em aberto:** um freio de VOLUME na entrada (adição em massa numa carga) continua não existindo em lugar nenhum. |
| D26 | **Toda mudança em interface ENTRE COMPONENTES começa pelo teste** (03/09/2026) — regra, não recomendação | Não é disciplina genérica de TDD: é uma regra sobre **um tipo específico de mudança**, e vem de placar medido num único dia. Ao trocar a entrega de arquivo por anexo nativo, o `teste_anexo_nativo.py` — escrito **antes** de publicar — pegou **três** defeitos que a revisão de código não pegou: (1) **contrato de prosa** — eu removi a frase `"Link para download:"` do retorno da tool, sem saber que o pipe faz `if "Link para download" in saida` (`chatnd.py:5773`) para decidir a oferta de outros formatos; teria desligado a oferta **em produção, em silêncio**; (2) **aliasing** — a lista de arquivos era devolvida por referência, e o payload do 1º evento ganhava o arquivo do 2º; (3) **parâmetro no meio da assinatura** — os dois novos campos deslocaram `imagens` para quem chama por posição, quebrando as suítes de imagem. **Por que só o teste pega:** interface entre componentes não está documentada em lugar nenhum — o contrato do "Link para download" existia como UMA linha de `if` num arquivo diferente, em outro módulo. Nenhuma leitura do arquivo alterado o encontraria; só exercitar o par encontra. **Corolário do D22** (o que precisa ser exato é imposto pelo código): o teste é a forma de descobrir **o que já estava sendo imposto** e você não sabia. **O que conta como interface entre componentes:** retorno de tool consumido por pipe ou por modelo; formato de carimbo lido por outro script; fatia/config duplicada entre repos; assinatura de função chamada por posição; nome de campo que o modelo preenche. **Vale também para dados**, e a mesma sessão provou: trocar as fixtures `ACERVOS/` por caminhos reais revelou que 9 de 19 caminhos do `mapa_assuntos.json` apontavam para pastas renomeadas — a etiqueta de assunto estava valendo zero havia semanas, com o `DIAL_FASE3` ligado. **Fixture que descreve um mundo que não existe é pior que teste ausente: ela dá confiança falsa e esconde o defeito que deveria denunciar.** |
| D27 | **Operação destrutiva roda simulação antes — independente de autorização** (04/09/2026) | Autorização é permissão para agir, **não dispensa de verificar**. Na remoção das 48 certidões o Davi autorizou executar direto; a simulação (`dry_run`) foi rodada mesmo assim e **pegou uma divergência**: o plano removia **2** arquivos de `nd-projetos` e a lista prevista tinha **1**. Os dois estavam certos (o segundo era um IPTU sob pasta `DUO DILIGENCE`), mas isso **só se soube conferindo** — antes da simulação, a diferença era indistinguível de um falso positivo prestes a apagar material legítimo. O custo da simulação são dois minutos; o custo de descobrir depois é irreversível. Vale para: remoção de índice, exclusão de arquivo, mudança de valve que afeta classificação, e qualquer `--confirmar-*`. |
| D28 | **Órfão de índice: as vias de nascimento, e por que "o freio avisa" não fecha o problema** (04/09/2026) | **Órfão** = arquivo no índice que o estado desejado não contém. Descoberto por acidente: o plano de remoção das certidões expôs `RTF-003 › Relatorio_Certidao_Imob`, **que estava no índice e não no repositório** — e junto vieram CNH, dois extratos bancários e três certidões, todos só no índice. Apareceram porque casavam com a regra nova; **órfão que não casa com regra nenhuma continua invisível**. **Vias mapeadas no código:** (1) **rodada barrada** — o freio aborta a rodada INTEIRA, nada é escrito, o repo anda e o índice congela; (2) **coleção PULADA** (`id "PREENCHER-APOS-CRIAR"`) — a rodada segue e as remoções daquela coleção nunca acontecem; (3) **falha de API na remoção** — levanta e aborta a rodada, mesma consequência de (1); (4) **`freio_mudanca_colecao`** — arquivo que troca de coleção pode ficar nas duas; (5) **vetores sem vínculo** — classe diferente, já coberta pelo `higiene_owui.py`. **A boa notícia:** (1), (3) e (4) são **auto-corrigíveis** — o plano é recalculado do zero a cada rodada, então a remoção pendente volta a ser planejada. **O que falha não é o plano, é a rodada não completar.** Foi o caso aqui: as coleções não recebiam escrita desde 22/08. **Por isso o alarme certo não é "houve bloqueio", é "não há rodada bem-sucedida há N horas"** — essa única checagem cobre freio, falha de API, workflow desabilitado e secret ausente, com um fato só. **DEFEITO ENCONTRADO no ciclo da issue:** o passo *"Rodada OK → fecha a issue de bloqueio"* (`sincronizar.yml:123`) **não tem guarda de `dry_run`**. Uma simulação com plano limpo **fecha a issue de bloqueio sem ter escrito nada** — a dívida sai do radar sem ter sido paga. Corrigir junto com o alarme de defasagem. **FECHADO em 04/09/2026 — ZERO órfãos.** O relatório rodou como Action (`orfaos.yml`, só leitura, credencial nos secrets) e as sete coleções batem exatamente com o repositório: **nd-atas 108, nd-projetos 17, nd-fonte 83, nd-normas 286, nd-marca 4, nd-contratos 5, nd-externo 20** — 523 arquivos, **nenhum com sinal de dado pessoal**. O resultado confirma a via (1) e só ela: não havia classe escondida nem defeito de plano, havia **acúmulo de 22/08 até 04/09**, drenado de uma vez quando a rodada completou com confirmação. **Órfão nasce de rodada que não completa, não de falha no plano** — e é isso que faz do alarme de defasagem o controle certo, e não um segundo varredor rodando em paralelo. Estas contagens são a **linha de base do que "limpo" significa**: a referência contra a qual o alarme e qualquer conferência futura se medem. (O defeito do `dry_run` foi corrigido no PR #147.) |
| D29 | **Credencial que já existe no lugar certo não se copia para o lugar conveniente** (04/09/2026) | Para rodar o relatório de órfãos, o primeiro passo a passo que escrevi pedia ao Davi definir `OPENWEBUI_BASE_URL` e `OPENWEBUI_API_KEY` no terminal dele. Os dois **já eram secrets do repositório desde 06/07** — os mesmos com que a esteira sincroniza todo dia. O runbook criava uma **segunda cópia** da credencial, num lugar sem rotação, sem auditoria e sem revogação centralizada, para fazer o que o GitHub já fazia. Correção: o relatório virou workflow manual (`orfaos.yml`) e ninguém manuseia token para investigar. **O teste que a regra impõe:** antes de pedir uma credencial a alguém, procurar onde ela já está — se existe num cofre que o executor alcança, o trabalho é **levar a execução até a credencial**, nunca a credencial até a execução. **Corolário operável:** runbook que começa com "cole o token" é sinal de que a execução está no lugar errado — trate como defeito de desenho, não como inconveniência. Palavras do Davi: _"eu ia criar uma segunda cópia da credencial num lugar sem rotação nem auditoria, para fazer o que o GitHub já fazia"_. |
| D30 | **Ferramenta de relatório: o achado não vai para o log, e encontrar não é falhar** (04/09/2026) | Duas decisões tomadas ao pôr o relatório de órfãos numa Action. Nenhuma é sobre órfãos — as duas são sobre **como um relatório se comporta**, e valem para o próximo. **(1) O nome do achado fica fora do log.** Órfão é, por definição, o arquivo que **não** está no repositório — e foi exatamente ali que apareceram CNH e extratos bancários. Nome de arquivo de dado pessoal **já é** dado pessoal: `CNH-<nome>.pdf` identifica a pessoa sem que ninguém abra o arquivo. E log de Action se lê de passagem, sem intenção e por qualquer um com acesso ao repo. O modo `--resumo` manda **só contagens** para o log; os nomes vão para o artefato, que exige download deliberado e expira em 3 dias. **(2) Job verde ao encontrar.** O código de saída passou a ser 0 = limpo, **2 = achou**, 1 = quebrou. Sair 1 nos dois casos era ambíguo (o `SystemExit` da variável ausente também sai 1), mas o motivo principal não é técnico: **relatório que fica vermelho ao cumprir a função treina todo mundo a ignorar o vermelho.** Vermelho tem de significar uma coisa só — a ferramenta não funcionou. O que ela **encontrou** vai para o Summary da página do run, legível sem abrir log nenhum. |
| D31 | **Alarme de defasagem: mede trabalho pendente, e o N deriva da cadência** (04/09/2026) | Fecha o vão do D28. **A pergunta é uma só** — _existe mudança esperando indexação há mais de N horas?_ — e cobre freio disparado, falha de API, workflow desabilitado e secret vencido, porque os quatro produzem o mesmo fato observável. **O que se mede importa mais que o limite:** medir _tempo desde a última rodada_ alarma em todo fim de semana quieto; a condição exige **commit de `.md` mais novo que a última escrita**, então silêncio legítimo nunca toca. Alarme que toca à toa é desligado — e alarme desligado ainda figura na doc como proteção, o que é pior que não ter. **N = 12h, com o número que o escolheu** (30 dias, medidos em 04/09 sobre o `puxar_sharepoint`, que é o caminho real da sincronização diária — o `sincronizar.yml` roda só 12 vezes no mesmo período): **133 rodadas, 26 falhas (19,5%), das quais só 3 ISOLADAS** (07/08, 12/08, 23/08); as outras 23 vieram em bloco de dias seguidos. Com rodada a cada 6h, **N=6h não tem margem de retentativa** — a falha isolada já deixa o commit pendente ~6h até a rodada seguinte consertar, e as 3 do mês virariam 3 falsos alarmes. **N=12h é um ciclo completo de retentativa.** Retrospectiva contra o histórico real: teria disparado em **24/08**, onze dias antes da descoberta, com zero disparos nos dias saudáveis. **Regra que fica:** N é derivado da cadência (2 × intervalo entre rodadas), não escolhido a gosto — se a cadência mudar, o N muda junto. **Canal:** issue única atribuída, não e-mail — notificação de workflow agendado vai para quem editou o arquivo por último, o que troca o destinatário por acidente a cada PR. A issue fecha sozinha na primeira rodada de escrita, então "aberta" significa uma coisa só. **Limite conhecido:** rodada bem-sucedida não é prova de escrita (um `puxar` sem delta sai cedo e conta como sucesso) — o alarme mede o processo, não o resultado; fechar de vez pede carimbo de escrita no `_estado/`. |
| D32 | **O caso “NÃO CIRCULAM” era falso positivo — e nome de pasta não é política de acesso** (04/09/2026, corrige a leitura vigente desde 17/08) | Os sete documentos da pasta `5 - Minutas a convergência - NÃO CIRCULAM` foram tratados por **três semanas** como incidente de acesso. Não eram. Levantado o contexto: `“NÃO CIRCULAM”` é instrução de **não distribuir para fora**, não de restringir acesso interno; quem marcou a pasta foi o **Sergio, CEO**; **todos os colaboradores têm visualização no SharePoint**; e quem usa o ChatND são esses mesmos colaboradores. **Não há ampliação de alcance** em indexar os sete — quem perguntaria ao ChatND já pode abrir os documentos hoje. Os sete **ficam** no índice e no repositório; nada a remover. **A lição, que é maior que o caso:** política de acesso mora no sistema de permissões, não no nome que alguém escreveu na pasta. Antes de tratar uma pasta com nome sugestivo como restrição, a pergunta é **quem tem permissão nela hoje** — se todo mundo tem, não há restrição a preservar. Um rótulo descreve intenção de quem escreveu; permissão descreve o fato. **Consequências:** PR #144 **fechado** (o código funcionava; a premissa é que estava errada), `eh_restrito` volta para a fila normal sem prioridade, `teste_restritos.py` fica segurado. **O que isto NÃO invalida:** as certidões, a CNH, os extratos e a árvore de DUE DILIGENCE eram **incidente real** — dado pessoal de terceiros, sem base para estar num índice que a empresa inteira consulta. As remoções dos PRs #141, #145, #146 e #150 seguem corretas. Só este caso era falso positivo. |
| D33 | **Número sem método envelhece virando folclore: todo número carrega data e método, ou não entra** (04/09/2026) — regra, não recomendação | Vale para comentário de código, doc viva, mensagem de commit e registro. **O caso concreto:** um comentário em `regras_governanca.py` afirmava _“medido contra 1426 nomes reais: 48 casamentos, nenhum falso positivo”_. Re-medindo no dia seguinte: **1166 nomes, 16 casamentos**. A queda é compatível com o que saiu do corpus (37 arquivos removidos, árvore de DUE DILIGENCE excluída), mas os dois levantamentos **não usam o mesmo método de extração** — então o par não é comparável termo a termo, e nenhum dos dois pode ser defendido sozinho. **Por que é pior que não ter número:** um número solto no código tem cara de prova. Ele encerra a discussão sem poder ser refutado, porque ninguém sabe como refazê-lo. **O formato mínimo:** _o quê, quanto, quando, sobre qual população, apurado como_. Se o método não couber na linha, cabe o ponteiro para o script que o produz. **Irmão do D24** (o valor efetivo mora no banco): lá a doc afirma configuração que ninguém conferiu; aqui, medição que ninguém consegue repetir. Os dois envelhecem em silêncio, e os dois se corrigem com a mesma disciplina: **afirmação em doc dura precisa dizer como foi verificada.** |
| D34 | **Antes de afirmar o que existe num branch, buscar — e o incidente que eu quase registrei** (05/09/2026) | Afirmei que `_f3_colecao` e `DIAL_FASE3` **não existiam na `main`** e concluí que o pipe de produção vinha de um branch marcado “não mergear/publicar” — ou seja, que a `main` não era a fonte da verdade do componente mais caro do sistema. **Falso.** O `origin/main` tem os dois, no mesmo arquivo e na mesma linha (`chatnd.py:3249`). O que eu tinha era um `main` **local 32 commits atrasado**, e um `git grep main` responde sobre a ref local, não sobre o remoto. **Não houve incidente de governança.** Consequências reais enquanto o erro esteve de pé: commitei um conserto no branch errado (revertido) e o Davi esteve a um passo de decidir entre “mergear o branch” e “republicar da main” para um problema inexistente. **A regra:** `git fetch` antes de qualquer afirmação sobre o que um branch contém, e preferir `origin/<branch>` a `<branch>` ao comparar — a ref local é uma foto com data, não o estado do projeto. **Irmão do D24 e do D33, e o mais desconfortável dos três:** lá a doc afirma configuração que ninguém conferiu e medição que ninguém repete; aqui a **ferramenta** respondeu com precisão sobre um mundo velho, e a precisão da resposta escondeu a idade da pergunta. **Registrado aqui, e não como incidente,** porque um incidente falso em doc dura é pior que doc ausente — daqui a seis meses alguém leria que a produção já rodou fora da `main` e agiria sobre isso. |
| D35 | **As bases do eixo novo ficam SEM descrição — com base = pasta, o nome já é a informação** (05/09/2026) | Foram escritas dez descrições (conteúdo, quando buscar, quando não buscar) e **nenhuma será aplicada**. **Por quê:** as bases vão crescer muito, e **descrição-inventário envelhece** — uma linha que lista o que a pasta contém hoje passa a mentir no dia em que alguém salva um arquivo diferente, e ninguém volta para corrigi-la. É o D33 aplicado a texto em vez de a número: afirmação sem manutenção vira folclore. E com o eixo por pasta o nome carrega o que precisa carregar — `Operações` diz o que `Operações` é. Sob o eixo por TIPO isso não valia, e por isso a descrição era necessária lá: `nd-normas` **não** dizia o que continha — dizia o contrário do que continha. **Trocar o eixo tornou a descrição dispensável, e não o inverso.** **A alternativa que fica para o futuro,** se a escolha de base pelo agente se mostrar ruim: **uma linha dizendo o que a pasta É, nunca o que ela contém** — definição de escopo, não inventário. Definição sobrevive ao crescimento; inventário não. **Como saber que chegou a hora:** o agente escolhendo a base errada de forma repetida em pergunta cuja pasta é óbvia para uma pessoa. |
| D36 | **Pasta-mãe que vai receber conteúdo ganha base ANTES, não depois** (05/09/2026) — reverte a decisão tomada horas antes | `Plataformas Regionais` tinha **um** arquivo, e ele era cópia de algo já indexado em Tecnologia. Com esse dado, decidi mantê-la **excluída**: criar base para hospedar uma cópia acrescentaria um nome a mais no `list_knowledge_bases` e uma chance a mais de o agente escolher errado, em troca de zero conteúdo novo. **O Davi reabriu, e está certo:** a pasta vai receber conteúdo próprio em breve, e *“a decisão de manter excluída foi tomada com o dado de hoje, e o dado de amanhã é diferente”*. **A regra que fica:** pasta-mãe vazia hoje que **vai** receber conteúdo ganha base **antes** — para o primeiro arquivo já entrar no lugar certo, em vez de cair na exclusão e só ser notado quando alguém procurar por ele e não achar. **O que eu errei, e vale mais que o caso:** decidi com o inventário de hoje uma questão que era sobre a **intenção de uso da pasta** — e essa informação não está no acervo, está em quem organiza o SharePoint. Contagem de arquivos responde “o que há”; não responde “o que vai haver”, e eu usei a primeira para responder a segunda. **Irmão do D35:** lá a decisão foi não descrever porque descrição-inventário envelhece; aqui, não decidir escopo por inventário pela mesma razão. **O único arquivo atual continua sendo cópia do que está em Tecnologia, e isso é a pasta refletida, não defeito** — a base espelha a pasta, e duplicata entre pastas é fato do SharePoint, não erro de roteamento. |
| D37 | **Conferência que compara zero com zero é verde permanente sem cobertura — e só rodar contra dado real, desconfiando do verde, pega** (05/09/2026) — palavras do Davi; a lição mais cara do conferidor | **O caso, e ele apareceu duas vezes no mesmo dia, por portas diferentes.** (1) A classe `base_indevida` procurava os nomes das pastas **declaradas como excluídas** dentro das contagens das coleções **configuradas**. Pasta excluída não tem coleção configurada, **por construção** — a interseção era sempre vazia. (2) A coleta lia `data.file_ids` de `/knowledge/{id}`, o campo **legado** que o `file/add` não atualiza: devolveria **zero** para toda base com arquivos vinculados, e zero é exatamente o valor que a classe considera correto. **Nos dois, a comparação era zero contra zero, e o resultado era verde permanente.** **Por que nenhuma revisão pega:** o código está certo linha a linha; o teste passa — as fixtures colocavam o mesmo nome nos dois lados, um mundo que não existe; e o relatório diz “nada encontrado”, que é a resposta que se espera de um sistema saudável. **Um teste com fixture inventada prova que o código faz o que o código faz.** **O que pega:** rodar contra dado real e, ao ver verde, **perguntar por que está verde**. Foi assim que os dois apareceram — o relatório voltou limpo e fui conferir o motivo do limpo. **A regra operável:** para toda conferência nova, escrever **o caso que a faria falhar** e prová-lo com dado do mundo real antes de confiar no verde; e desconfiar em especial de comparação cujo lado esperado é **vazio, zero ou ausente**, porque nela o acerto e a cegueira têm a mesma aparência. **Família:** irmão do D26 (fixture que descreve mundo inexistente dá confiança falsa) e do princípio de que **ferramenta de conferência que erra sozinha é pior que ferramenta ausente** — aqui ela não erra, ela **acerta sem olhar**, que é mais difícil de flagrar. |
| D38 | **Escrever a prova é a última conferência** (05/09/2026) — observação do Davi sobre o método, depois de o padrão se repetir dia após dia | **O padrão:** o defeito apareceu **enquanto se escrevia o registro**, e não enquanto se escrevia o código. Os casos que dá para apontar no documento: **04/09** — ao mapear as cinco vias de nascimento de órfão para o D28, apareceu que o passo *“Rodada OK → fecha a issue”* não tinha guarda de `dry_run`, e que a simulação prudente teria fechado a issue sem pagar a dívida; **05/09** — ao escrever o D37, ao invés de só citar o endpoint errado, fui conferir *por que* a classe estava verde e achei a segunda cegueira, a interseção vazia por construção; **05/09** — ao redigir o corpo do PR do conserto anterior, apareceu que a mensagem de “não conferida” culpava a credencial mesmo quando ela estava presente. **Por que funciona, e não é acaso:** escrever a prova obriga a responder *por que isto está certo* em vez de *isto parece certo*. A revisão lê o código e confirma que ele faz o que diz; a redação do registro obriga a defender que **o que ele diz é o que precisava ser feito** — e é nessa distância que o defeito mora. **O corolário prático:** o registro não é burocracia depois do trabalho; é **a última passada de conferência**, e por isso se escreve **antes** de dar a tarefa por encerrada, nunca depois. Um PR cujo corpo não consegue explicar por que a mudança está certa não está pronto para merge — mesmo com a suíte verde. **Fecha a família:** o D26 diz que teste pega o que leitura não pega; o D37 diz que rodar contra dado real pega o que teste não pega; este diz que **escrever pega o que rodar não pega** — porque rodar mostra o que acontece, e escrever obriga a dizer o que **deveria** acontecer. |

### INCIDENTE DE PRIVACIDADE — certidões de pessoas físicas indexadas (LGPD)

> **Registro cronológico**, mantido nesta forma porque a sequência de datas pode ser
> pedida. Tratamento interno conduzido pelo Davi; esta seção documenta o técnico.

| Quando | O quê |
|---|---|
| **03/09/2026** | **Entrada.** A reformulação das pastas do SharePoint (4 → 15 pastas-mãe) trouxe `Operações/` para o escopo. A carga automática do `puxar_sharepoint` (commit `171cee4`) ingeriu a árvore `Operações/2. Retrofit/RTF-001-Ipanema_Apto_Rio de Janeiro/03-DUE DILIGENCE SIMPLIFICADA/Certidões/` — **25 arquivos**. |
| 03/09/2026 | Os arquivos passaram a ser classificados para `nd-normas`, a coleção que **todo o ChatND consulta**. |
| **04/09/2026** | **Detecção.** Durante verificação das pastas soberanas pedida pelo Davi, a varredura do acervo encontrou a árvore. Medido: **24 dos 25 arquivos contêm CPF com dígito verificador válido**. |
| 04/09/2026 | **Contenção (regra).** PR #145 na esteira: exclusão da árvore inteira de due diligence, por segmento de pasta. |
| 04/09/2026 | **Quarto caso.** O detector de entrada (PR #146) encontrou `DOCUMENTOS PARA DUO DILIGENCE` — grafia com erro — em **quatro** operações, mais **22 arquivos** que a primeira versão da regra não pegava. Regra corrigida no mesmo PR #145. |
| 04/09/2026 | **Remoção do índice: EXECUTADA.** 37 arquivos removidos (35 `nd-normas`, 2 `nd-projetos`), **zero falhas**, com dry-run antes conforme D27. A conferência de órfãos do mesmo dia deu **zero** — ver D28. |

**Natureza do dado:** CPF e situação cadastral, CND federal, certidão de dívida ativa estadual, certidões cíveis — de **pessoas físicas nomeadas nos próprios nomes de arquivo**, incluindo uma subpasta *"Certidões Cônjuge"*. São **terceiros** de uma operação de retrofit: não são coautores, não têm relação com a plataforma, e não foram informados de que os documentos alimentariam um sistema de busca corporativo.

**Alcance:** os documentos ficaram recuperáveis por qualquer usuário do ChatND, via busca semântica e `grep_knowledge_files`, entre 03/09 e a remoção.

**Causa raiz (mesma de outros dois casos no mesmo período):** `_scripts/escopo.json` é lista de **inclusão por pasta-mãe, recursiva**. Pasta-mãe nova traz junto tudo que estiver abaixo dela, sem inspeção. Os outros dois: minutas *"NÃO CIRCULAM"* (7 arquivos, **três semanas** no ar, 17/08 a 04/09) e notas fiscais nominais de coautores (23 arquivos, PR #141).

**Agravante estrutural (D25):** os três freios da esteira calculam sobre **remoção**. Nenhum olha para adição. A ingestão indevida não disparou nada.

**Medidas tomadas:** regra de exclusão por pasta (#145) · detector de dado pessoal na entrada, com modo de varredura sobre todo o acervo (#146) · guarda das pastas soberanas no portão do sincronizar (#143).

**Limite conhecido:** o detector varre o **repositório**. Documento que esteja no índice e não no repo (resto de carga antiga) exige a conferência com credencial (`conferir.py`) — não foi executada.

**Execução da remoção (04/09/2026, 17h UTC):** PR #145 mergeado e `sincronizar.yml` disparado manualmente com `confirmar_remocao_em_massa` — necessário porque o plano gerava **8 eventos** de remoção contra um limite de 5, e sem a confirmação a rodada abortaria sem escrever nada. Resultado: **37 arquivos removidos, zero falhas** — 35 de `nd-normas` e 2 de `nd-projetos`; as outras cinco coleções intactas. Foram 37 e não 48 porque **11 nunca chegaram ao índice** (entraram no repo na carga do dia, antes de a regra existir): não estavam expostos, e a regra os barra permanentemente.

**O que a remoção expôs, e não estava na varredura do repositório:** `CNH-e.pdf`, dois extratos bancários (`Fatura_Itau`, `FaturaIndividualAgo`) e três certidões (`CNDT`, `consulta protestos`, `certidão de elementos fiscais`) — **existiam só no índice**. Ver D28.

**Ressalva sobre o armazenamento — inalcançável não é apagado.** O `delete_file=true` (que o `sincronizar` usa) apaga os vetores da coleção, a coleção vetorial própria do arquivo e o **registro em `Files`**, que é onde vive o nome — então nome e conteúdo saem da busca. **O blob no Storage/R2 não é apagado por esse caminho.** Ele fica sem registro que o referencie, portanto inalcançável pela aplicação — mas continua existindo no bucket. Para quem for decidir sobre retenção: **a diferença entre "não é mais recuperável pelo sistema" e "não existe mais" é real**, e só a segunda satisfaz um pedido de eliminação. Decisão não tomada em 04/09.

**Recomendação a quem cuida do `FAZ_DueDiligenceDocumental`:** se um dia esse documento precisar voltar à base, a saída certa **não** é afrouxar a regra — é **remover os CPFs do documento no SharePoint**. A análise dominial cumpre sua função identificando a proprietária pelo nome e pela matrícula; o CPF dela e o do cônjuge não acrescentam nada à análise e são o que impede a indexação. Mesmo raciocínio vale para qualquer documento de análise que cite terceiros.

## Parte 2 — Pendências que precisam de decisão ou ação

### Prioridade definida pelo usuário
1. ✅ **Separação de memória (Postgres)** — CONCLUÍDA em 2026-07-02 (Postgres + pgvector + R2). Ver [07_Diario_e_Status](07_Diario_e_Status.md).
2. 🟠 **Volume / billing do Railway** — o volume antigo segue montado como backup; avaliar desmontá-lo após período de segurança. Billing OK ($3/$20).
3. 🟡 **Grupos / permissionamento** — controle de acesso mais fino (plano esboçado: grupos Externo/Interno, base pública sem v30).

### Fase 1 (2→7 coleções) — status para o Davi (2026-08-18)
- 🟠 **Publish único** do ciclo Fase 1 (pipe 1.64.0 + fatia 1.63.2): criar as 7 coleções no painel, preencher `MAPA_COLECOES` (json papel→id) e `BASE_CONHECIMENTO_ID`, então publicar. Até lá produção roda a fatia antiga e `MAPA_COLECOES` vazia (dormente).
- ✅ **Etiquetas no contexto injetado** (procedência externa do `nd-externo`, `status=rascunho` do v31, `tipo=convergencia`) — implementadas no 1.64.0 (`_etiquetas_trecho`), entram no mesmo publish.
- ✅ **Fiação do `sincronizar.py`** (T4) — implementada (D-a/D-b/D-c): partição por `colecao_destino` + espelho + freio proporcional; dormente até trocar o `sync_config.json` pelo exemplo da Fase 1.
- 🟡 **Confronto repo × produção**: rodar `migrar_sete_colecoes.py` com `NIDUM_URL`+`NIDUM_TOKEN` (GET) para a lista nominal do que existe só de um lado (o Davi executa com credencial).
- 🟢 **`id vivo` de `BASE_CONHECIMENTO_ID`**: o default morto virou vazio; o id vivo mora no painel.
- 🟢 **`nd-fonte` = 9 obras canônicas** (decisão do Davi): FONTE é curada e pequena; a base nova espelha o repo. Os **61** arquivos legado da "ND Fonte" de produção (só em produção, não no repo) **não migram** — ficam na coleção antiga. Catálogo para revisão no fim do projeto: `esteira/_docs/FASE1_CATALOGO_LEGADO_FONTE.md` (grupo ⭐ = possível doutrina a trazer para o repo).
- ✅ **CORREÇÃO (2026-08-23) do item acima — os 61 JÁ ESTÃO no repo.** Conferência nome a nome (normalizando maiúsculas/pontuação): dos 61 listados no catálogo, **zero** estão ausentes do `FONTE/` do repo (hoje **72** `.md` = os 61 + 8 canônicas + 3 obras que chegaram depois do catálogo). Não foi migração manual: o `git log -- FONTE/` mostra só `puxar_sharepoint: carga automatica (delta)` — entraram no SharePoint e a esteira puxou pelo caminho certo. Cruzamento independente: o `_estado/sharepoint_delta.json` lista os mesmos 70. **Pendência que a conferência levantou:** as canônicas eram 9 e hoje são 8 — o `Nidum Documento Fundador - v31 (rascunho).md` foi removido do repo pela própria esteira (commit `ac3052e`), espelhando uma remoção no SharePoint. Confirmar se foi intencional. **Consequência:** a coleção antiga "ND Fonte - Fundadores" deixou de ser cópia única de qualquer coisa, o que remove o principal impedimento para aposentá-la (ver D19). O catálogo na esteira foi carimbado.
- 🟢 **Higiene (sem agir agora):** 4 comentários pré-existentes não-ASCII no `chatnd.py` (linhas ~298/329/411/1561) violam a convenção ASCII-only de `_nidum_tools/*.py`; antecedem a Fase 1. Corrigir no **próximo ciclo de manutenção do pipe**, não agora.

### Retenção de conversas (spec decidida 2026-07-02 — a IMPLEMENTAR)
- **Objetivo:** gestão de armazenamento.
- **Regra 1 — comprimir:** chat **sem interação há 7 dias** → comprimir o conteúdo para ocupar menos espaço.
- **Regra 2 — deletar:** chat **sem interação há 90 dias** → deletar o chat **+ os arquivos associados**.
- **Natureza:** funcionalidade nova de backend (Open WebUI não tem nativo) — job agendado sobre a tabela `chat` do Postgres, com **dry-run** antes de qualquer exclusão. A parte dos 90 dias é destrutiva → backup/confirmação.
- **Decisão em aberto:** "comprimir" = compressão real do conteúdo (exige o app descomprimir na leitura, mexe no fork) **ou** arquivar (nativo, mais simples). A decidir antes de implementar.

### `MODELO_GERAL` aponta para `nidum-10---dia-a-dia` — decisão pendente (05/09/2026)

> **Não mexi no código, de propósito.** O default não é verificável offline, e
> trocar um default que eu não consigo conferir seria repetir exatamente o D24.

O que o código diz (`chatnd.py:4179-4182`), e o comentário é explícito: o apontamento
**é deliberado** — `MODELO_GERAL` usa o mesmo wrapper que era o “Dia A Dia” porque
**o Gerador de Arquivos já está anexado a ele**, e criar wrapper novo exigiria lembrar
de reanexar a tool: *“é o clique que ninguém lembra”*.

O que não sei: **se esse wrapper ainda existe no painel.** Na sessão A.2 apareceram
modelos revogados e renomeados, e este é alcançado por `bypass_filter=True`, sem passar
por `process_chat_payload` — ou seja, um wrapper revogado não produziria erro visível na
rota, só comportamento diferente.

**As três saídas, e o que cada uma custa:**

1. **Confirmar no painel que o wrapper existe** e deixar como está. Custo: um olhar.
   É a opção certa se ele existe — o motivo original continua válido.
2. **Criar wrapper próprio para `geral`** e reanexar o Gerador. Custo: o clique que o
   comentário diz que ninguém lembra — e, se esquecido, o `geral` perde a geração de
   arquivos **em silêncio**. Só vale se houver razão para os dois divergirem.
3. **Esvaziar o default** (como se fez com `BASE_CONHECIMENTO_ID`) e obrigar o painel a
   decidir. Custo: instalação nova quebra até alguém preencher — que é a intenção:
   falhar alto em vez de apontar para lugar errado.

Minha leitura, sem decidir: **(1) se o wrapper existe, (3) se não existe.** A (2) só se
houver produto por trás, e não para arrumar nome.
### Outras pendências em aberto
| Pendência | Natureza | Observação |
|---|---|---|
| **Default de `BASE_CONHECIMENTO_ID`** aponta para base morta (`f2c8a48c`) | Dívida técnica | Só funciona porque a valve sobrescreve para `a85d8a8f`. Alinhar o default no código. |
| **PDFs órfãos do v29/v30** (cópias fora da base do ChatND) | Decisão | Mantidos por regra `#não exclua nada`; remover só com OK explícito. |
| **`Capture001.png` (9,45 MB)** no volume | Decisão | Não removido (screenshot ambíguo, possível anexo de chat). |
| **Mecanismo de higiene de duplicados recorrente** | Implementação | **Desenhado** em `_nidum_manutencao/HIGIENE_DUPLICADOS.md`, não implementado (em stand-by por decisão do usuário). |
| **Editorial F3 — vínculo "modelo por projeto"** | Implementação + deploy | Núcleo da ficha pronto; falta o manifold no seletor. |
| **Editorial F2.4b — imagens nos exports** | Implementação | Embutir imagens com alt-text nos `.docx/.epub/.pdf`. |
| **`origin/main` atrás do main local** | Sincronização | Um deploy foi via `railway up` do local; sincronizar o GitHub com `git push origin main`. |
| **Repasse no roteador** (rota documentos sem trecho relevante deveria cair para diaadia) | Implementação | Hoje, sem contexto relevante, ainda manda para Documentos sem handoff. |
| **Sobreposição rápido × dia a dia** | Decisão | Fronteira tênue; usuário cogitou afiar ou fundir. |
| **SharePoint como fonte (auto-update)** | Projeto futuro | Exige app no Azure AD + Microsoft Graph + job de sync. |
| **Modelo guard de input/output** | Segurança | Recomendado antes de abrir uploads/conteúdo externo (injeção indireta). |
| **Pipe × arquivos da pasta** (D20) | Implementação | O middleware injeta os arquivos da pasta em `form_data['files']` a **cada** mensagem, e o `_anexos_recentes` não distingue origem. Três riscos: TRAVA 5 (anexo + transformação → rota `arquivo`) disparando sozinha; `MAX_CHARS_ANEXO` sendo pago em todo turno; e a regra "acervo pulado quando há anexo" desligando o RAG institucional em silêncio dentro de um projeto. Conserto proposto: marcar os arquivos vindos da pasta (cruzar ids com `folder.data['files']` via `__metadata__`) e tratá-los como canal próprio — "material do projeto" ≠ "anexo deste turno". **Medir antes de mexer** (log: `classificador=`, `chars_acervo`, linha "COM anexo"). |

> Conforme as decisões forem tomadas, mover da Parte 2 para a Parte 1 (com a data e o porquê).
